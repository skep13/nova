Character artwork for the orb, optional.

Drop files here, then redeploy:
  default.webp   (or .png)  - required; used for every state
  idle.webp      listening.webp   thinking.webp   speaking.webp   - optional

Transparent background, portrait crop, roughly 700x900, under ~300 KB each.
With no files present the page falls back to the orb and nothing breaks.
